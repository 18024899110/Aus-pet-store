## Vercel Frontend Environment Variables
## Copy these to Vercel Project Settings → Environment Variables:

# Backend API URL - Update with your Railway backend URL after deployment
REACT_APP_API_URL=https://your-backend.railway.app/api/v1

# App Configuration
REACT_APP_APP_NAME=CY Pet Store
REACT_APP_VERSION=1.0.0

# Production settings
REACT_APP_DEBUG=false
NODE_ENV=production
GENERATE_SOURCEMAP=false

# Optional: Analytics (get these IDs from Google Analytics/Sentry)
# REACT_APP_GA_ID=
# REACT_APP_SENTRY_DSN=
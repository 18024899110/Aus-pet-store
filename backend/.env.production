# Production Environment Configuration
PROJECT_NAME=CY Pet Store
API_V1_STR=/api/v1

# Security Settings - IMPORTANT: Change these in production!
SECRET_KEY=your-super-secret-key-change-this-in-production-make-it-long-and-random
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# CORS Settings - Replace with your actual frontend domain
CORS_ORIGINS=https://your-frontend.vercel.app,https://cy-pet-store.vercel.app

# Database Settings - Railway PostgreSQL (recommended)
# Get this from Railway PostgreSQL addon
SQLALCHEMY_DATABASE_URI=postgresql://username:password@hostname:port/database

# Admin Settings - IMPORTANT: Change default password!
ADMIN_EMAIL=admin@cypetstore.com
ADMIN_PASSWORD=SecurePassword123!

# Email Settings (Optional - for order confirmations)
SMTP_TLS=true
SMTP_PORT=587
SMTP_HOST=smtp.gmail.com
SMTP_USER=noreply@cypetstore.com
SMTP_PASSWORD=your-app-password
EMAILS_FROM_EMAIL=noreply@cypetstore.com
EMAILS_FROM_NAME=CY Pet Store

# File Upload Settings
UPLOAD_FOLDER=static/uploads
MAX_CONTENT_LENGTH=16777216

# Production Settings
ENVIRONMENT=production
DEBUG=false